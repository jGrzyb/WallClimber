using UnityEngine;

/// <summary>
/// Marker on the root of the inference-only scene. Prevents automatic parallel training
/// bootstrap so a single agent and follow-camera behaviour are used.
/// </summary>
public class WallClimberInferenceScene : MonoBehaviour
{
}
